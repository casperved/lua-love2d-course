-- Assignment 01: Hello, World!
-- Your very first LÖVE2D program.
-- Fill in the three TODOs inside love.draw() and run it!

function love.load()
    -- Runs once when the program starts.
    -- Nothing to set up for this assignment.
end

function love.update(dt)
    -- Runs every frame (~60 times per second).
    -- Nothing to update yet — but LÖVE2D needs this function to exist.
end

function love.draw()
    -- Runs every frame to paint the screen.
    -- Rule: always call setColor BEFORE print, so the text gets the right color.

    -- TODO 1: Set the color to red (1, 0, 0) and print "Hello, World!" at (100, 100).

    -- TODO 2: Set the color to sky blue (0, 0.5, 1) and print your name at (100, 150).

    -- TODO 3: Pick any color and print any message at (100, 200).
end
